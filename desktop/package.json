{
  "name": "scribeflowai-desktop",
  "private": true,
  "version": "0.1.61",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test:smoke": "npm run build && cargo check --manifest-path src-tauri/Cargo.toml",
    "preview": "vite preview",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "release:macos": "bash ./scripts/macos-release.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/plugin-dialog": "^2.6.0",
    "@tauri-apps/plugin-fs": "^2.4.5"
  }
}
